replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 80

ingress:
  enabled: true
  className: nginx
  hostname: "" # will be set via nip.io or workflow
  path: /
annotations:
  kubernetes.io/ingress.global-static-ip-name: gke-static-ip
